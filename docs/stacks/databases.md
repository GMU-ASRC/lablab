# databases

`core-postgres` and `core-valkey`, no host ports; reachable
only over `core-data`. `kaneo` and `coder` both use `core-postgres`, see
["If a stack needs a database"](../networking.md#if-a-stack-needs-a-database)
for how to add another.
